### Sprint 1: Project Setup and Core Asset Management

- **Project Initialization:** Set up a new Vite project with React and TypeScript.
- **Dependencies:** Installed and configured `tailwindcss`, `postcss`, `autoprefixer`, `axios`, and `react-router-dom`.
- **Project Structure:** Created a clean project structure with `components`, `pages`, and `services` directories.
- **UI Layout:** Implemented a main `Layout` component and set up initial routing.
- **Asset Listing:** Developed the `AssetsPage` to fetch and display a list of assets from the API, including loading and error states.
- **Add Asset Feature:** Implemented the "Add Asset" functionality using a modal form, which successfully creates new assets and refreshes the list.


### Sprint 2: Employee Management

- **API Service:** Extended the API service with `getEmployees`, `createEmployee`, and `getEmployeeAssets` functions.
- **Employees Page:** Created a new page to list all employees, with options to add a new employee and view their assigned assets.
- **Add Employee Feature:** Implemented the "Add Employee" functionality using a modal form, which successfully creates new employees and refreshes the list.
- **View Employee Assets:** Added functionality to view the assets assigned to a specific employee in a modal.
- **Routing & Navigation:** Added a new `/employees` route and updated the main navigation to include a link to the new page.


